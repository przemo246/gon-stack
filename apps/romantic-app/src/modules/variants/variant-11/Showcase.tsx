import ShowcaseTemplate from '../shared/ShowcaseTemplate';

export default function Showcase() {
  return (
    <ShowcaseTemplate
      variantNumber={11}
      variantTitle="Variant 11: Minimal Monochrome"
      fontPair="Work Sans + Open Sans"
      styleLabel="Strict grayscale, clean grid, and minimal UI with no color distraction."
    />
  );
}
