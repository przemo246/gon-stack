import ShowcaseTemplate from '../shared/ShowcaseTemplate';

export default function Showcase() {
  return (
    <ShowcaseTemplate
      variantNumber={20}
      variantTitle="Variant 20: Mint Fresh"
      fontPair="Outfit + Literata"
      styleLabel="Mint and white for a clean, refreshing, minimal interface."
    />
  );
}
